---
all:
  children:
    boat_nodes:
      hosts:
        boat-node:
          # IP corrente del Pi — cambia ad ogni sessione in fase di sviluppo
          # (hotspot telefono, wifi di casa, o qui via cavo ethernet diretto).
          # Da aggiornare prima di ogni sessione (vedi README).
          ansible_host: 192.168.2.2
          ansible_user: fkimn3rd
      vars:
        ansible_python_interpreter: /usr/bin/python3
