---
all:
  children:
    boat_nodes:
      hosts:
        boat-node:
          # Hostname mDNS: roles/base imposta l'hostname a "boat-node" e
          # installa/abilita avahi-daemon, che risponde su *.local sulla
          # rete locale — funziona su qualunque rete il Pi raggiunga (wifi
          # di casa, hotspot, cavo diretto) senza dover cercare l'IP a mano
          # ad ogni sessione.
          ansible_host: boat-node.local
          ansible_user: fkimn3rd
      vars:
        ansible_python_interpreter: /usr/bin/python3
